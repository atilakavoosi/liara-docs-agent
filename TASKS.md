# TASKS

> **آخرین جایی که موندیم:** بلوک ۵ (امنیت/مانیتورینگ/هزینه) تموم شد.
> `/api/health` دیگه وضعیت واقعی ایندکس رو می‌ده، rate limit با burst
> واقعی تست شد (۴۲۹ دقیقاً سر درخواست ۱۳ام)، کش معنایی + `/admin`
> ساخته و با Playwright واقعی تست شدن. **یافته‌ی مهم:** آستانه‌ی کش
> معنایی از ۰.۹۵ به ۰.۹۷ اصلاح شد چون اندازه‌گیری واقعی نشون داد ۰.۹۵
> ریسک جواب غلط داشت (ADR-008) — کش الان محافظه‌کارانه‌ست (فقط تکرار
> تقریباً دقیق سؤال، نه بازنویسی آزاد). دیپلوی روی لیارا **هنوز انجام
> نشده** (حساب شارژ نشده، به تصمیم آتیلا برای مرحله‌ی آخر موکول شد).
> قدم بعدی: بلوک ۶ — polish و دمو (favicon از قبل هست، چک بشه)، یا
> بلوک ۲.۵ (eval عددی) اگه وقت موند.

## In Progress
- [ ] بلوک ۶ — polish و دمو

## Backlog

### بلوک ۲.۵ — eval عددی (باقی‌مونده از بلوک ۲، ~۱ ساعت)
- [ ] `scripts/eval.ts` + ~۳۰ سؤال طلایی، گزارش recall@5 روی `hybridSearch`
- [ ] rerank با مدل ارزان (اختیاری، اگه eval نشون داد لازمه)

### بلوک ۶ — polish و دمو (~۵ ساعت)
- [ ] favicon و متادیتای صفحه (فایل `app/favicon.ico` از قبل هست — بررسی کن که واقعیه نه پیش‌فرض Next)
- [ ] seed data / سؤال‌های پیشنهادی (چهارتا همین الان توی EmptyState هست، کافیه؟)
- [ ] ویدیوی دمو (بیمه‌ی قطعی اینترنت)
- [ ] README یک‌صفحه‌ای نهایی با لینک دمو
- [ ] `npm audit` نهایی + تست موبایل و تب ناشناس

## Done
- [x] بلوک ۵ — `/api/health` واقعی (نه هاردکد)، کش معنایی
      (`lib/cache.ts`، بازپخش با پروتکل رسمی AI SDK)، ثبت مصرف
      (`lib/usage.ts`)، داشبورد `/admin` پشت `ADMIN_TOKEN`. rate limit
      و کش هر دو با تست واقعی end-to-end تأیید شدن (نه فقط کد خونده
      شد). یک باگ UI واقعی (padding جدول) با اسکرین‌شات پیدا و رفع شد.
      ADR-008 مستند شد. دو تا کامیت لازم (کد + مستندات).
- [x] بلوک ۴ — shadcn/ui (radix-nova + RTL)، پالت واقعی برند لیارا،
      Vazirmatn، چت کامل با ۶ tool renderer اختصاصی، پنل منابع، دارک‌مود،
      موبایل. با Playwright تست شد نه فقط ساخته شد.
- [x] بلوک ۱ — ۱۱۴۲ سند دانلود، ۲۳۵۸ تکه، BM25 + جستجوی برداری + RRF
- [x] بلوک ۳ — provider (AvalAI)، ۶ ابزار agentic، `/api/chat` با
      streamText چندمرحله‌ای، rate limit، لاگ ساخت‌یافته — تست واقعی موفق
- [x] فاز صفر: تحلیل مسئله، scope، DoD، نقشه‌ی معیار داوری → `docs/PROJECT.md`
- [x] تحقیق پلتفرم: کشف کورپوس markdown، تأیید embedding، تأیید نسخه‌های Node
- [x] اسکلت Next 16 + Tailwind + TS، `npm audit` تمیز
- [x] `liara.json` با healthCheck، `.env.example`، `/api/health`
- [x] ADR-001 تا ۰۰۸

## Blocked
- دو چالش دیگه‌ی هکاتون — منتظر شرح چالش از آتیلا
