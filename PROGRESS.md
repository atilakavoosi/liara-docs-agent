# PROGRESS

لاگ append-only. ورودی جدید به انتهای فایل اضافه می‌شه.

---

## ۱۴۰۵/۰۵/۲۹ — ۲۰۲۶-۰۸-۲۰ — بلوک ۰: تحقیق و اسکلت‌بندی

**چیکار شد:**
- تحقیق پلتفرم لیارا. یافته‌ی تعیین‌کننده: لیارا کل مستنداتش رو به‌صورت
  markdown تمیز منتشر می‌کنه — `docs.liara.ir/all-links-llms.txt` شامل
  **۱۱۴۲ لینک `.md`** ـه و هر فایل خط `Original link:` داره که URL منبع
  citation رو مجانی به‌مون می‌ده. یعنی نه crawler لازمه نه HTML parsing.
  این بزرگ‌ترین ریسک پروژه بود و قبل از نوشتن کد حذف شد.
- تأیید شد سرویس AI لیارا embedding داره (`openai/text-embedding-3-small`)
  و baseUrlش `https://ai.liara.ir/api/v1/<projectId>` سازگار با OpenAI ـه.
- اندازه‌گیری کورپوس با نمونه‌گیری ۲۵ فایلی: میانگین ۴.۷KB، مجموع ~۵.۴MB.
  همین عدد باعث شد وکتوراستور استاتیک به pgvector ترجیح داده بشه (ADR-003).
- اسکلت `create-next-app` + Tailwind + TypeScript.
- `docs/PROJECT.md` (فاز صفر)، `docs/DECISIONS.md` (ADR-001..004)،
  `CLAUDE.md`، `TASKS.md`، `liara.json`، `.env.example`، `/api/health`.

**چی شکست / چی غافلگیرکننده بود:**
- `create-next-app@15` با **۳ آسیب‌پذیری high** نصب شد (postcss و sharp)
  که فقط با Next 16 برطرف می‌شن. پلن اولیه عمداً Next 15 رو انتخاب کرده بود
  تا از ریسک ناسازگاری با build لیارا دوری کنه. تصمیم عوض شد: چون اپ هنوز
  خالی بود، ارزون‌ترین لحظه برای تست Next 16 همین بود. بعد از ارتقا
  `npm audit` می‌ده `found 0 vulnerabilities` و build محلی موفقه (ADR-002).
- صفحه‌ی مستندات مدل‌های OpenAI لیارا لیست مدل‌ها رو سمت کلاینت رندر می‌کنه
  (در markdown فقط `{item}` دیده می‌شه). پس شناسه‌ی دقیق مدل‌ها از env
  خونده می‌شه و باید با لیست واقعی کنسول تأیید بشه.

**قدم بعدی:**
اثبات مسیر دیپلوی روی لیارا (push → build → live) قبل از نوشتن هر کد دیگه‌ای.
تا وقتی این مسیر تأیید نشده، هیچ فیچری ساخته نمی‌شه.

---

## ۱۴۰۵/۰۵/۲۹ (ادامه) — ۲۰۲۶-۰۸-۲۰ — بلوک ۱، ۳: بازیابی و لایه‌ی Agentic

**چیکار شد:**
- کورپوس کامل (۱۱۴۲ سند) دانلود و به ۲۳۵۸ تکه تبدیل شد. دو مشکل کیفیت
  کشف و رفع شد: یک تصویر base64 با ۴۸ هزار کاراکتر که یک تکه رو بلعیده
  بود، و اینکه کلمه‌ی «دیپلوی» در **صفر** سند وجود داشت (مستندات
  «استقرار» می‌نویسه) — بدون نگاشت هم‌معنی، رایج‌ترین سؤال ممکن هیچ
  نتیجه‌ای نمی‌داد.
- ایندکس برداری کامل ساخته شد (۲۳۵۸ × ۱۵۳۶، ۱۴MB) و کامیت شد.
- **تغییر مسیر ارائه‌دهنده‌ی AI:** به‌جای سرویس AI خود لیارا، از AvalAI
  استفاده شد (کلید از قبل آماده بود). ADR-006. کد چون از یک provider
  عمومی سازگار با OpenAI استفاده می‌کنه، این تغییر فقط env بود، نه کد.
- لایه‌ی agentic کامل: ۶ ابزار (searchDocs، askClarification،
  diagnoseError، generateConfig، buildRunbook، draftSupportTicket) و
  `/api/chat` با حلقه‌ی چندمرحله‌ای.
- **تست end-to-end واقعی** (نه فقط typecheck) روی سؤال «چطور Next.js
  دیپلوی کنم»: مدل جستجو کرد، سند واقعی quick-start پیدا کرد، با
  buildRunbook مراحل رو ساخت، جواب نهایی با ارجاع [۱][۲] و لینک واقعی
  docs.liara.ir داد، و قدم بعدی («شناسه اپت رو بگو تا liara.json بسازم»)
  پیشنهاد داد. دقیقاً سناریوی #۱ از Definition of Done.

**چی شکست / غافلگیرکننده بود:**
- در تست اول، مدل فکر کرد پیام کاربر «به‌هم‌ریخته» ـه و به‌جای جستجو
  سراغ askClarification رفت. بررسی نشون داد این باگ کد نبود — از
  `curl -d` با متن فارسی inline در Git Bash روی ویندوز بود که encoding
  رو خراب می‌کرد. با `--data-binary` از یک فایل UTF-8 واقعی، مشکل رفع
  شد. **نکته برای بعد:** برای تست دستی API با متن فارسی، همیشه از فایل
  استفاده کن، نه `-d` با رشته‌ی inline توی شل ویندوزی.
- `convertToModelMessages` در این نسخه از AI SDK **async** ـه (برخلاف
  انتظار اولیه) — باید await بشه.
- در ادغام نتایج (RRF)، بدون محدودیت تنوع منبع، تمام ۵ نتیجه‌ی برتر
  یک سؤال از یک سند تکراری میومدن. با محدود کردن به حداکثر ۲ تکه از هر
  sourceUrl رفع شد.
- Next 16 خودش یه بلوک هشدار به CLAUDE.md اضافه می‌کنه («این Next.js
  که می‌شناسی نیست») و می‌گه قبل از کدنویسی `node_modules/next/dist/docs/`
  رو چک کن. بررسی شد: تغییرات بزرگ (`middleware`→`proxy`، async
  params/cookies/headers) هیچ‌کدوم به کد فعلی ما ربط ندارن چون هنوز
  middleware یا dynamic route نداریم. یادداشت شد برای وقتی که لازم بشه.

**تصمیم مهم غیرفنی:** دیپلوی روی لیارا به‌درخواست آتیلا به مرحله‌ی آخر
موکول شد چون حساب هنوز شارژ نشده. این ریسک از قبل در `docs/PROJECT.md`
شناسایی شده بود («Always Be Demoable») و آگاهانه پذیرفته شد — نه فراموشی.

**قدم بعدی:** بلوک ۴ — UI. بک‌اند کامل کار می‌کنه ولی هیچ رابط چت واقعی
روی صفحه نیست؛ این بزرگ‌ترین شکاف بین «کار می‌کنه» و «قابل دمو»ست.

---

## ۱۴۰۵/۰۵/۲۹ (ادامه) — ۲۰۲۶-۰۸-۲۰ — بلوک ۴: UI کامل + سه باگ واقعی

**چیکار شد:**
- قبل از کد زدن، پالت رنگ و فونت واقعی خود لیارا رو از CSS واقعی
  `console.liara.ir` استخراج کردم (نه حدس زدم) — mint `#87fcc4`، آبی
  `#28c1f5`، خاکستری-سرمه‌ای تیره `#23252d`. فونت واقعیشون «Yekan Bakh»
  بود ولی لایسنسش برای بازتوزیع نامشخص بود، پس Vazirmatn (SIL OFL،
  ۱۰۰٪ آزاد) رو با وزن‌های متنوع جایگزین کردم. همه در `docs/DESIGN.md`.
- shadcn/ui نسخه‌ی جدیدشون (`radix-nova` preset) رو با `--rtl` نصب کردم.
- چت کامل: `chat-shell.tsx` + `message-item.tsx` + ۶ کامپوننت اختصاصی
  برای هر tool (نه JSON خام) + `sources-panel.tsx` (عنصر امضا، طبق
  DESIGN.md) + `markdown.tsx` با citation badge و کدبلاک.
- تست واقعی با Playwright (نه فقط typecheck) روی چند سناریو: حالت خالی،
  سؤال ساده تا جواب کامل، دارک‌مود، موبایل، و سؤال مبهم → کلیک گزینه →
  ادامه‌ی مکالمه.

**سه باگ واقعی که فقط با تست دستی پیدا شدن:**

۱. **حلقه‌ی بی‌نهایت رندر.** افکت استخراج پروفایل (`platform`/`service`
   از ورودی toolها) روی `[messages]` بود، که در حین استریم ده‌ها بار در
   ثانیه رفرنسش عوض می‌شه — هر بار `setProfile` یه آبجکت تازه می‌ساخت.
   جواب: فقط وقتی `status === 'ready'` اجرا بشه، و قبل از `setState`
   مقدار قبلی رو چک کنه.

۲. **جهت RTL روی بلوک کد.** چون کل صفحه `dir="rtl"` ـه، بلوک‌های کد
   (JSON، بش) بدون `dir="ltr"` صریح، ترتیب خط‌ها و آکولادهاشون به‌هم
   می‌ریخت — یک باگ کلاسیک RTL+کد که فقط با اسکرین‌شات واقعی دیده شد،
   نه با خوندن کد. جواب: `dir="ltr"` روی `<pre>` و `<code>`.

۳. **«Tool result is missing».** `askClarification` عمداً بدون
   `execute` طراحی شده (UI باید مکث کنه). ولی این یعنی در تِرن دوم،
   تاریخچه‌ی مکالمه یک tool-call بدون نتیجه داشت و API ردش می‌کرد.
   این باگ فقط با یک سناریوی **دوتِرنی واقعی** خودش رو نشون داد —
   نه با curl تکی، نه با typecheck. جواب: `addToolResult` قبل از فرستادن
   پیام بعدی کاربر (ADR-007).

**نکته برای بعد:** هر سه باگ رو فقط تست end-to-end با مرورگر واقعی
پیدا کرد، نه typecheck و نه تست تکی API. برای هر مسیر agentic جدید،
حداقل یک سناریوی دوتِرنی رو دستی امتحان کن.

**قدم بعدی:** بلوک ۵ — امنیت/مانیتورینگ (rate limit موجوده ولی تست
نشده)، کش معنایی، داشبورد `/admin`.

---

## ۱۴۰۵/۰۵/۲۹ (ادامه) — ۲۰۲۶-۰۸-۲۰ — بلوک ۵: امنیت/مانیتورینگ/هزینه

**چیکار شد:**
- رفع باگ کهنه: `/api/health` به‌جای `"index": "pending"` هاردکد،
  الان از `getIndex()` واقعی می‌خونه (تعداد chunk، لود شدن بردارها).
- کش معنایی (`lib/cache.ts`) با بازپخش واقعی از پروتکل رسمی AI SDK
  (`createUIMessageStream`/`createUIMessageStreamResponse`) — نه یک
  فرمت SSE دست‌ساز — تا کلاینت هیچ فرقی بین جواب زنده و کش‌شده حس نکنه.
- ثبت مصرف (`lib/usage.ts`) و داشبورد `/admin` (پشت `ADMIN_TOKEN`):
  کل درخواست‌ها، نرخ اصابت کش، توکن ورودی/خروجی، جدول درخواست‌های اخیر.
  هزینه‌ی تخمینی عمداً «تنظیم نشده» نشون داده می‌شه تا وقتی قیمت واقعی
  مدل در env نیاد — به‌جای یک عدد ساختگی.
- rate limit با یک burst واقعی ۱۵تایی تست شد: دقیقاً درخواست ۱۳ام
  ۴۲۹ گرفت، با پیام فارسی درست و هدر `Retry-After`.

**چی شکست / غافلگیرکننده بود:**
- **آستانه‌ی کش معنایی ۰.۹۵ (طبق پلن اولیه) عملاً ناامن بود.** قبل از
  قبولش، با فراخوانی واقعی `embed()` چند جفت سؤال فارسی سنجیده شد: جفت
  هم‌معنی («قیمت» / «هزینه») فقط ۰.۸۴ داد، ولی جفت‌های **متفاوت** و
  خطرناک («پلن پایه» / «پلن حرفه‌ای» = ۰.۸۸، ECONNREFUSED / ECONNRESET
  = ۰.۸۷) از همون هم‌معنی بالاتر بودن. یعنی هیچ آستانه‌ی تکی نیست که
  بازنویسی واقعی رو بگیره و سؤال نزدیک-ولی-غلط رو رد کنه — یک کش
  معنایی واقعی با این embedding روی سؤال کوتاه فارسی ایمن نیست.
  آستانه به ۰.۹۷ تغییر کرد (فقط تکرار تقریباً دقیق رشته، نه بازنویسی
  آزاد) — مستند در ADR-008. این دقیقاً همون نوع فرضی بود که اگه بدون
  اندازه‌گیری قبول می‌شد، می‌تونست وسط دمو جواب غلط به یه سؤال بده.
- تست اول جدول `/admin` با Playwright false-negative داد: سلکتور
  `text=ورود` هم به تیتر کارت («ورود به داشبورد ادمین») هم به دکمه
  می‌خورد و روی تیتر کلیک می‌کرد. با `getByRole('button', {name:...})`
  رفع شد — باگ تست بود، نه اپ.
- ولی همون اسکرین‌شات یک باگ واقعی UI رو لو داد: ستون‌های جدول
  `/admin` بدون padding افقی بودن، تا جایی که «gpt-5.6-terra» با عدد
  توکن ورودی مجاور بصری قاطی شده بود و غیرقابل‌خوندن می‌شد. با
  `px-2` روی سلول‌ها رفع شد.

**نکته برای بعد:** یک عدد «منطقی به‌نظر می‌رسه» (مثل آستانه‌ی ۰.۹۵) رو
بدون اندازه‌گیری روی داده‌ی واقعی قبول نکن — فاصله‌ی بین «درسته» و
«خطرناک» می‌تونه خیلی کمتر از انتظار باشه.

**قدم بعدی:** بلوک ۶ — polish و دمو: favicon (از قبل هست، چک شه)،
ویدیوی دمو، README نهایی، `npm audit` نهایی. دیپلوی همچنان به تصمیم
آتیلا موکول شده تا حساب لیارا شارژ بشه.

---

## ۱۴۰۵/۰۵/۲۹ (ادامه) — ۲۰۲۶-۰۸-۲۰ — بلوک ۶: favicon واقعی + README + اسکرین‌شات

**چیکار شد:**
- **favicon واقعی ساخته شد.** موقع چک کردن `app/favicon.ico` («از قبل
  هست، فقط بررسی کن») معلوم شد همون مثلث پیش‌فرض Vercel/Next بود که از
  روز `create-next-app` هیچ‌وقت عوض نشده بود — دقیقاً همون چیزی که داور
  گفته بود نباید باشه. با همون گلیف واقعی هدر (`MessageSquareText` از
  lucide) روی پس‌زمینه‌ی `#0ea36f` (mint تیره‌شده‌ی برند)، `app/icon.svg`
  ساخته شد. از همون SVG، `favicon.ico` (چندسایز، ۱۶/۳۲/۴۸، با یک
  ICO-packer دستی چون هیچ کتابخونه‌ای نصب نبود) و `app/apple-icon.png`
  (۱۸۰×۱۸۰) با `sharp` رندر شدن. با رندر واقعی مرورگر (نه فقط باز کردن
  فایل) تأیید شد هر سه درست نمایش داده می‌شن، و با curl تأیید شد Next
  هر سه تگ `<link rel="icon">` رو درست تولید می‌کنه.
- **README پروژه کامل بازنویسی شد** — قبلش متن پیش‌فرض `create-next-app`
  بود (یعنی هیچ‌کس تا الان بازش نکرده بود، یک تلِ AI-slop دیگه). الان
  شامل: مسئله با کلمات پروژه، ۶ ابزار agentic، معماری بازیابی، بخش
  امنیت/هزینه، استک، Definition of Done، و یک بخش صادقانه‌ی «محدودیت‌های
  شناخته‌شده» (ایندکس استاتیک، آستانه‌ی محافظه‌کارانه‌ی کش، بدون حافظه‌ی
  بین‌جلسه‌ای).
- **اسکرین‌شات واقعی** با Playwright گرفته شد (نه mockup): حالت خالی
  روشن/تاریک، موبایل، و یک مکالمه‌ی واقعی کامل («اتصال دامنه‌ی سفارشی»)
  که ۶ منبع واقعی با نوار امتیاز، ارجاع درون‌متنی [۱][۲]، و پیشنهاد قدم
  بعدی رو نشون می‌ده — دقیقاً عنصر امضای طراحی‌شده در DESIGN.md. چهارتا
  از این‌ها در `docs/screenshots/` کامیت شدن و توی README جاسازی شدن.
- `.gitignore` مشکلی که در بلوک ۵ کشف شده بود (`.env*` بدون استثنا
  `.env.example` رو هم قایم می‌کرد) اینجا دوباره تأیید شد که درست کامیت
  شده.
- `npm run build` و `npm audit` نهایی: هر دو تمیز.

**چی شکست / غافلگیرکننده بود:**
- دوباره: چک کردن ادعای «favicon از قبل هست» به‌جای قبولش، یک باگ واقعی
  و مهم پیدا کرد (لوگوی Vercel، نه لیارا) — درست مثل الگوی تکرارشونده‌ی
  این پروژه که هیچ ادعایی بدون شواهد قبول نمی‌شه.
- بین این بلوک و بلوک قبلی، سرور dev محلی (که پس‌زمینه اجرا شده بود)
  از بین رفته بود (هیچ پروسه‌ی node‌ای زنده نبود) — دوباره راه‌اندازی
  شد. یادآوری: سرور dev پس‌زمینه بین سشن‌ها/فشردگی‌های context تضمینی
  زنده نمی‌مونه، قبل از هر تست زنده وضعیتش چک بشه.
- کش معنایی (که در بلوک ۵ تست شده بود) دیگه اون entry قدیمی رو نداشت —
  چون سرور واقعاً ری‌استارت شده بود، نه فقط hot-reload؛ رفتار درسته
  (حافظه‌ی in-memory قرار بوده با عمر پروسه یکی باشه)، فقط یادآوری که
  دمو نباید فرض کنه کش از یک تست قدیمی هنوز زنده‌ست.

**تصمیم مهم:** ویدیوی دمو نمی‌تونه از این محیط (بدون دسترسی به صفحه‌نمایش
واقعی/میکروفون کاربر) ضبط بشه — این کار دستی برای آتیلاست. آماده‌سازی
(اسکریپت پنج‌سناریوی DoD، اپ کاملاً کار می‌کنه، UI صیقلی) تموم شده؛ فقط
خود ضبط مونده.

**قدم بعدی:** یا بلوک ۲.۵ (eval عددی recall@5، ~۱ ساعت) اگه وقت موند،
یا مستقیم دیپلوی روی لیارا وقتی حساب شارژ شد — که در اون نقطه README
باید لینک دمو زنده رو هم بگیره.

---

## ۱۴۰۵/۰۵/۲۹ (ادامه) — ۲۰۲۶-۰۸-۲۰ — بلوک ۲.۵: eval عددی بازیابی

**چیکار شد:**
- `scripts/eval.ts` نوشته شد: ۳۴ سؤال طلایی، همه‌شون از URLهای **واقعی**
  استخراج‌شده از `data/chunks.json` (نه ساختگی)، پوشش هر ۱۰ دسته‌ی
  سرویس کورپوس (ai، dbaas، paas، one-click-apps، dns، email-server،
  object-storage، references، iaas، mirrors). چندتا سؤال عمداً با کلمه‌ی
  محاوره‌ای «دیپلوی» نوشته شدن (نه «استقرار») تا SYNONYMS واقعاً تست بشه.
- روی ایندکس واقعی اجرا شد: **recall@5 = 85.3% (۲۹ از ۳۴)**.

**یافته‌ی واقعی (نه حدس):** هر ۵ سؤال ناموفق یک الگوی مشترک دارن — کورپوس
لیارا صفحه‌های هم‌نام رو **در هر سرویس جداگانه تکرار می‌کنه** (مثلاً
`change-plan` هم زیر `paas/details/` هست هم `iaas/details/` هم
`email-server/details/` هم `ai/details/`؛ همین‌طور `limitations` و
صفحات team). وقتی کوئری سرویس رو صریح نمی‌گه، BM25 و بردار هر دو بین
این نسخه‌های هم‌نام گیج می‌شن و گاهی نسخه‌ی سرویس اشتباه رو بالاتر
می‌برن. این eval روی `hybridSearch` خام (بدون فیلتر service/platform)
اجرا می‌شه — یعنی **بدترین حالت واقعی**: در اپ زنده، profile-boost
(وقتی از مکالمه معلوم شده کاربر روی کدوم سرویسه) دقیقاً همین مورد رو
بهتر می‌کنه، پس عدد ۸۵.۳٪ کف واقعیه، نه سقف.
- تصمیم: به‌جای «رفع» این مورد زیر فشار وقت (که نیاز به قانون یا
  rerank جدید داره)، صادقانه مستند شد به‌عنوان محدودیت شناخته‌شده و
  مسیر بهبود آینده. عدد الان (۸۵٪) برای دمو کافیه؛ سناریوهای DoD همه
  سرویس رو صریح می‌گن (مثلاً «برنامه Next.js روی لیارا») که این ابهام
  رو ندارن.

**قدم بعدی:** بلوک ۲.۵ کامل شد. باقی‌مونده‌ی کل پروژه فقط ویدیوی دمو
(دستی، بیرون از این محیط) و خود عملیات دیپلوی (منتظر شارژ حساب) ـه.

---

## ۱۴۰۵/۰۵/۲۹ (ادامه) — ۲۰۲۶-۰۸-۲۰ — جلسه‌ی ایده‌پردازی فاز بعد (بدون کد)

**چیکار شد:** طبق درخواست صریح آتیلا، نقش «Chief Creative Technologist +
داور باتجربه‌ی هکاتون» گرفته شد. زمینه از `PROJECT.md`/`DECISIONS.md`/
`DESIGN.md`/`TASKS.md` خونده شد، بعلاوه‌ی خوندن مستقیم کد فعلی
(`tool-parts.tsx`، `chat-shell.tsx`، `empty-state.tsx`، `lib/usage.ts`،
`lib/ai/tool-types.ts`) تا هیچ ایده‌ای روی چیزی که از قبل هست یا وجود نداره
حدس زده نشه. با WebSearch واقعی (۹ جستجو) تحقیق شد: خودِ هکاتون استارکوچ×
لیارا (تأیید شد چالش رو خودِ لیارا داوری می‌کنه؛ هیچ آرشیو برنده‌ی قبلی
پیدا نشد — صادقانه ثبت شد به‌جای حدس)، و قرض از صنعت‌های دیگه با شواهد واقعی
(Intercom Fin، Glean، LogRocket/Sentry، چک‌لیست هوانوردی، quest log بازی،
Vercel v0).

۲۰ ایده‌ی خام تولید و با ماتریس Impact/Effort فیلتر شد؛ ۸ تا رد شدن با دلیل
صریح (یکی‌شون چون مستقیم نقض «Explicitly Cut» در PROJECT.md بود). ۸ ایده‌ی
برتر با فرمت کامل (یک‌خطی/چرا نو نیست/چرا امتیاز می‌ده/Effort/ریسک/حس UX) در
`docs/FEATURE_IDEAS.md` ذخیره شد. صادقانه علامت‌گذاری شد که کدوم ایده‌ها
واقعاً تازه‌ن و کدوم‌ها الگوی رایج‌ان (مثل بازخورد یک‌کلیکی) که فقط برای
enabler-بودن ارزش دارن، نه ادعای نوآوری.

**پیشنهاد Signature Feature:** «رادار شکاف مستندات» — لیدربرد سؤال‌های
بی‌جواب/کم‌اطمینان در `/admin`، از داده‌ای که همین الان مجانی تولید می‌شه.
دلیل انتخاب به‌جای «چراغ صداقت» (که نمره‌ی خامش بالاتره): این یکی مستقیم
کاربر ثانویه‌ای که PROJECT.md از روز اول اسمش رو آورد (تیم پشتیبانی لیارا)
رو به یک فیچر قابل‌دیدن تبدیل می‌کنه — و چون خودِ لیارا داوره، این دقیقاً
همون چیزیه که یادشون می‌مونه.

**قدم بعدی:** طبق دستور صریح آتیلا در همین درخواست، **هیچ کدی نوشته
نشد و نمی‌شه** تا وقتی که خودش از بین این ۸ ایده انتخاب کنه. `TASKS.md`
Backlog با پوینتر به فایل آپدیت شد، هیچ‌چیز «Done» علامت نخورد چون هنوز
هیچ‌کدوم ساخته نشده.

---

## ۱۴۰۵/۰۵/۳۰ — ۲۰۲۶-۰۸-۲۱ — تاریخچه‌ی چندگفتگویی محلی (فوری، خارج از FEATURE_IDEAS)

**چیکار شد:** آتیلا یک مشکل واقعی گزارش داد که در هیچ‌کدوم از ۸ ایده‌ی
جلسه‌ی قبل نبود: بدون تاریخچه، اگه سؤال یک کاربر تکرار بشه، باید دوباره
از صفر مشکلش رو تعریف کنه، به‌جای برگشتن به گفتگویی که قبلاً همون مشکل
رو حل کرده بود. این مستقیم به تصمیم Explicitly Cut در `PROJECT.md`
(«تاریخچه‌ی ماندگار بین دستگاه‌ها») برخورد داشت، پس قبل از کد زدن، دقیق
سنجیده شد که چیزی که Cut شده بود *بین دستگاه‌ها* بود، نه تاریخچه به‌طور
کلی — یک تاریخچه‌ی فقط-همون-مرورگر (localStorage) بدون حساب کاربری،
همون تصمیم رو دست‌نخورده نگه می‌داره. ADR-009 نوشته شد.

**ساخته شد:**
- `lib/conversation-store.ts` — CRUD محض روی localStorage (حداکثر ۲۰
  گفتگو، FIFO)، دقیقاً هم‌الگو با پروفایل کاربر که از قبل بود.
- `Header` یک dropdown «تاریخچه» گرفت (شمارنده، عنوان خودکار از اولین
  پیام کاربر، زمان نسبی، دکمه‌ی حذف).
- `ChatShell` یک `conversationId` نگه می‌داره؛ بعد از هر پیام (فقط وقتی
  استریم تموم شده) گفتگو کامل ذخیره می‌شه — شامل تمام tool call/result،
  نه فقط متن، پس بازیابی یعنی همون چیزی که قبلاً حل شده بود عیناً برمی‌گرده
  (منابع، runbook، هرچی).

**تست واقعی (نه فقط typecheck):** با یک اسکریپت Playwright — سؤال اول →
آیتم تاریخچه ظاهر شد → «گفتگوی جدید» پاکش کرد → کلیک روی تاریخچه پیام‌ها
و پنل منابع رو کامل برگردوند. صفر خطای کنسول. **یافته‌ی جانبی:** اولین
تلاش‌های تست به‌خاطر یک full-reload از Fast Refresh (که با ادیت فایل‌های
دیباگ تو همون پوشه‌ی watched خودش تداخل داشت، نه یک باگ محصول) fail
می‌شد؛ با یک تأخیر settle قبل از تعامل حل شد. فایل‌های موقت دیباگ حذف
شدن، هیچ‌کدوم کامیت نشدن.

`npm run build` تمیز، TypeScript بدون خطا.

**قدم بعدی:** طبق backlog قبلی — دیپلوی (منتظر شارژ حساب) و ویدیوی دمو.
اگه آتیلا از بین ۸ ایده‌ی `docs/FEATURE_IDEAS.md` انتخاب کرد، همون
مسیر (TASKS → ADR → کد) دوباره طی می‌شه.
